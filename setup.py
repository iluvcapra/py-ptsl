#!/usr/bin/env python

from setuptools import setup
from ptsl import __VERSION__

setup(name='ptsl',
      version=__VERSION__,
      packages=['ptsl'],
      scripts=[],
      package_data={},
      )